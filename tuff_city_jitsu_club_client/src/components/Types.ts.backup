export type Permission = string;

export type User = {
    firstName: string;
    lastName: string;
    permissions: Permission[];
}

// Following seems to be superfluous code from a similar example
// export type ElementType = {
//     name: string;
//     price: number;
//     currency: 'EUR' | 'USD';
// }